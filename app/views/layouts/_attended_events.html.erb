<div id="attended-events-container">
  <% if current_user%>
    <h1>Attended Events</h1>

    <ul>
      <% current_user.attended_events.each do |event| %>
        <li><%= link_to event.name, event_path(event) %></li>
      <% end %>
    </ul>
  <% end %>
</div>